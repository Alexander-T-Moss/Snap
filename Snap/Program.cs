using System.Collections;

namespace Snap
{
    class Program
    {
        public static void Main()
        {

            Console.Write()

            /*
            Deck testDeck = new();
            Console.WriteLine("Looking at the Card: " + testDeck.GetTopCard().GetNumber() + " of " + testDeck.GetTopCard().GetSuit());

            testDeck.Shuffle();
            Console.WriteLine("Looking at the Card: " + testDeck.GetTopCard().GetNumber() + " of " + testDeck.GetTopCard().GetSuit());

            
            while(true)
            {
                if(Console.ReadKey().Key != ConsoleKey.Enter)
                {
                    Console.Write("You didn't press enter :(");
                    break;
                }
            }

            foreach(Card card in testDeck)
            {
                Console.WriteLine(card.GetNumber());
            }
            // create queue
            // add element(s) to queue
            // check first element and queue size
            // remove element
            // check first element and queue size

            Queue testQueue = new();

            testQueue.enqueue(6);
            testQueue.enqueue(9);
            testQueue.enqueue(3);

            Console.WriteLine(testQueue.size());

            Console.WriteLine(testQueue.first());

            testQueue.dequeue();

            Console.WriteLine(testQueue.size());

            Console.WriteLine(testQueue.first());



            Stack reversingStack = new();

            string originalString = "Stacks";

            foreach (char letter in originalString)
            {
                reversingStack.Push(letter);
            }

            string reversedString = "";

            for (int x = reversingStack.Size(); x > 0; x--)
            {
                reversedString += reversingStack.Top();
                reversingStack.Pop();
            }

            Console.Write(reversedString);
            */
        }
    }
}